import styled from "styled-components";

export const Container = styled.div``;

export const Content = styled.div`
  display: flex;
  justify-content: space-between;

  //ajustando os cards para não força ficar na mesma lina,
  flex-wrap: wrap;
  
`;
